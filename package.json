{
  "name": "dotenv-configserver",
  "version": "1.0.0",
  "description": "Load remote config to GITHUB_ENV from a configserver repository",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Steph0/dotenv-configserver.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript",
    "Dotenv",
    "Configserver"
  ],
  "author": "Steph0",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Steph0/dotenv-configserver/issues"
  },
  "homepage": "https://github.com/Steph0/dotenv-configserver",
  "dependencies": {
    "@actions/core": "^1.2.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0",
    "eslint": "^7.4.0",
    "jest": "^26.6.3"
  }
}
